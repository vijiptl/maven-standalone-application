pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                echo 'Cloning the Git repository...'
                checkout scm
            }
        }
    }
}

